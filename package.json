{
  "name": "netslim",
  "version": "1.0.0",
  "description": "PID-based process-level network simulation CLI with bandwidth throttling, latency, and packet loss",
  "main": "dist/index.js",
  "bin": {
    "netslim": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "lint": "eslint src/**/*.ts",
    "test": "jest"
  },
  "keywords": [
    "network",
    "throttling",
    "traffic-control",
    "tc",
    "pf",
    "cgroup",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
